# Northstar Release Desk

Northstar is a fictional release dashboard used only for the SwiftAgent demo.
The `release_badge` function formats a small text badge for a local UI.

Run the acceptance tests with:

```bash
python3 -m unittest -q
```

The initial implementation is intentionally incomplete. No package install or
network access is needed.
