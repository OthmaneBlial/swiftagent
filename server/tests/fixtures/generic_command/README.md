# Generic command fixture

`fake_text_agent.py` implements stdin and discrete-argument prompts, a free
version probe, bounded stderr, non-zero exit, timeout, and output-limit
scenarios. It uses only the Python standard library and never performs network
or model calls.
