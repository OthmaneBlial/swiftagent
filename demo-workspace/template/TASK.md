Fix `release_badge.py` so every test in `test_release_badge.py` passes.

Constraints:

- inspect the README, implementation, and tests before editing;
- modify only `release_badge.py`;
- do not install packages or use the network;
- preserve the public function signature;
- run `python3 -m unittest -q`; and
- report the exact verification command and result without claiming success if
  the command did not pass.

If your adapter asks for approval before the file edit or test command, pause
for that native approval. If it does not support approvals, continue within the
listed scope and let SwiftAgent record that capability difference.
